<header class="bg-white shadow-sm py-4 self-stretch">
    <div class="container mx-auto p-1 flex justify-between items-center">
        <h1 class="text-xl font-bold text-blue-600">MySite</h1>
        <nav class="space-x-7">
            <a href="/home" class="<%= currentPage === 'home' 
            ? 'text-blue-600 font-medium p-2 rounded bg-blue-100' 
            : 'text-blue-600 font-medium p-2 rounded hover:bg-blue-100 ' %>">
                Home
            </a>
            <a href="/contact" class="<%= currentPage === 'contact' 
            ? 'text-blue-600 font-medium p-2 rounded bg-blue-100' 
            : 'text-blue-600 font-medium p-2 rounded hover:bg-blue-100 ' %>">
                Contact
            </a>
        </nav>
    </div>
</header>